// Supabase cloud sync module (currently disabled)
// To enable: install @supabase/supabase-js and configure VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY

export const isSupabaseConfigured = (): boolean => false
